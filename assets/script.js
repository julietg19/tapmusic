window.addEventListener("load", () => {
  const sounds = document.querySelectorAll(".sound");

  const pads = document.querySelectorAll(".pads div");

  // sound

  pads.forEach((pad) => {
    pad.addEventListener("click", function () {});
  });
});

// research arrow functions l
